# 远程 skill 缓存：能从 GitHub 重拉，不进仓库
store/cache/

# 本机联接视图：Windows junction 会被 git 当成普通目录，重复提交内容
_active/

# 提案是临时沟通载体，批准后删除，不留痕
proposals/

*.tmp
*.log

# 字节码缓存
__pycache__/
*.pyc

# skill-creator 之类工具的工作区产物（评测跑批、基准、反馈）
# 注意：工作区应当放在仓库外，这里是兜底
*-workspace/
