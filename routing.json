{
  "_comment": "挂载路由表。clients 是各 agent 的 skills 目录；mode=junction 走目录联接，mode=extraDirs 走原生配置（不建联接）。",
  "_version": 1,

  "clients": {
    "workbuddy": {
      "dir": "%USERPROFILE%\\.workbuddy\\skills",
      "mode": "junction",
      "enabled": true,
      "note": "只扫自己的目录，不认公共位"
    },
    "zcode": {
      "dir": "%USERPROFILE%\\.agents\\skills",
      "mode": "junction",
      "enabled": true,
      "note": "唯一认 ~/.agents/skills 公共位的 agent（黑盒实测确认）。它同时也扫 ~/.zcode/skills"
    },
    "opencode": {
      "dir": "%USERPROFILE%\\.config\\opencode\\skills",
      "mode": "junction",
      "enabled": true,
      "note": "npm 全局 CLI；目录原本不存在，首次 link 会自动创建"
    },
    "doubao": {
      "dir": "%USERPROFILE%\\DoubaoWork\\skills",
      "mode": "junction",
      "enabled": true,
      "note": "豆包工作的技能根目录（黑盒实测确认可识别）"
    },
    "qclaw": {
      "dir": "%USERPROFILE%\\.qclaw\\skills",
      "mode": "extraDirs",
      "enabled": false,
      "config": "%USERPROFILE%\\.qclaw\\openclaw.json",
      "note": "不纳入统一管理（用户已决定）：QClaw 那批多是它自己的预置 skill，保留其自带的 extraDirs 配置即可，本项目不接管"
    }
  },

  "defaults": {
    "clients": ["workbuddy", "zcode", "opencode", "doubao"],
    "comment": "默认把 store 里全部 skill 挂到这些 client。要裁剪请改 skills 段。"
  },

  "skills": {
    "_comment": "逐个 skill 的例外规则。未列出的走 defaults.clients；clients 写成空数组 = 完全不挂载。",
    "local-workbuddy-quirks": {
      "clients": ["workbuddy"],
      "why": "内容全是 WorkBuddy 宿主特有的（工具层输出拿不到、安全策略、shim 补丁、沙箱代理、pwsh 里的 git）。其他 agent 没有这些限制，读它纯属噪音甚至误导，所以只挂 WorkBuddy。"
    }
  },

  "projects": {
    "_comment": "项目级规则。绝大多数 skill 不需要在这里出现。match 支持 path_glob / has_file / exclude_glob。",
    "bindings": []
  }
}
